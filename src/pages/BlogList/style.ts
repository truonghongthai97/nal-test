import styled from 'styled-components';
import MediaObject from 'src/components/MediaObject';

export const StyledMediaObject = styled(MediaObject)`
  margin-bottom: 16px;
`;

export const ListWrapper = styled.div``;
